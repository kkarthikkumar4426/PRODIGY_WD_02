# Stopwatch Web App

Simple stopwatch built with HTML, CSS and JavaScript.

Quick start:

1. Open `index.html` in your browser, or run a simple HTTP server:

```bash
python -m http.server 8000
# then open http://localhost:8000 in your browser
```

Features:
- Start, Pause, Reset
- Record multiple lap times (displayed in the Laps list)
